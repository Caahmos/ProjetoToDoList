<%- include('includes/header') %>
    <%- include('includes/nav') %>

        <div class="inteiro d-flex justify-content-center align-items-center">
            <div class="cartao text-center">
                <%- include('includes/messages') %>
                <div class="head">
                    <h3 class="text-center">Editar Tarefa</h3>
                </div>

                    <div class="cartao-conteudo">
                        <table class="text-center">

                            <form action="/tarefa/editada/<%= tarefa._id %>" method="post">
    
                                <tr>
                                    <td><input class="input-editado form-control" value="<%= tarefa.tarefa %>" type="text" name="tarefa"
                                            id=""></td>
                                    <td>
                                        <select class="input-editado form-control" name="status">
                                            <option value="Não iniciada">Não iniciada</option>
                                            <option value="Em andamento">Em andamento</option>
                                            <option value="Concluída">Concluída</option>
                                        </select>
                                    </td>
                                    <td><button class="btn-primary form-control" id="enviar">Atualizar</button></td>
                                </tr>
    
                            </form>
                        </table>
                        <a class="text-white" href="/">Voltar</a>
                    </div>
            </div>
        </div>


        <%- include('includes/footer') %>